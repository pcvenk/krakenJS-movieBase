{>"layouts/master" /}

{<body}
    {#movies}
        <div class="medium-4 columns end">
            <img src={.cover}>
            <h3>{.title}</h3>
            <a href='/movies/details/{._id}' class=button>Info</a>
        </div>
    {:else}
        <p>No Movies in the Database!</p>
    {/movies}
{/body}
